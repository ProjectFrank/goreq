machine:
  environment:
    GOPATH: ~/.go_project/src/github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME:$GOPATH
dependencies:
  cache_directories:
    - ~/goreq/new_dir
    - new_dir
    - node_modules
    - ~/goreq/node_modules