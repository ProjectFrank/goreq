machine:
  environment:
    GOPATH: /home/ubuntu/.go_project:$GOPATH
    example: \`case $CIRCLE_NODE_INDEX in 0) 0 ;; 1) 1;; esac\`
dependencies:
  cache_directories:
    - ~/goreq/new_dir
    - new_dir
    - node_modules
    - ~/goreq/node_modules